package controle;import Model.*;import controle.*;import limite.*;public class ControlePrincipal {    //Controladores    public ControleImovel ctrImovel;    public ControleProposta ctrProposta;    public ControleVisita ctrVisita;    public ControleCorretor ctrCorretor;        public ControleVendedor ctrVendedor;    public ControleComprador ctrComprador;    //Limites    public LimitePrincipal lmtPrincipal;    public LimiteRelatorio lmtRelatorio;    public CatalogoImoveis lmtCatalogo;        public ControlePrincipal() {        //Inicializa os controladores fazendo com que possamos contrala-los        //por meio do controle principal        ctrImovel = new ControleImovel(this);        ctrProposta = new ControleProposta(this);        ctrVisita = new ControleVisita(this);        ctrCorretor = new ControleCorretor(this);                ctrVendedor = new ControleVendedor(this);          ctrComprador = new ControleComprador(this);        //Inicia o menu principal        lmtPrincipal = new LimitePrincipal(this);        lmtRelatorio = new LimiteRelatorio();        lmtCatalogo = new CatalogoImoveis(this);    }    public static void main(String[] args) {        new ControlePrincipal();    }}